node_modules/
dist/
build/
coverage/
.vercel/

# env
.env
.env.local
.env.*.local

# generated credentials — never committed, shared only via the submission email per the brief
docs/CREDENTIALS.local.md
docs/CREDENTIALS.local.json

# supabase local
supabase/.branches/
supabase/.temp/

# editor/OS
.DS_Store
Thumbs.db

# logs
*.log
npm-debug.log*

# stryker
.stryker-tmp/
reports/mutation/

# generated — run `npm run sync:domain` before `supabase functions deploy`
supabase/functions/_shared/domain/
